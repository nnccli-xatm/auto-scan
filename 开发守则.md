# 扫描设备自动任务系统 - 开发守则

**版本**: v1.0  
**制定日期**: 2026-06-11  
**性质**: 长期执行原则，贯穿项目全生命周期  
**适用范围**: 所有项目参与者、AI助手、开发团队

---

## 核心开发原则

### 原则一：计划执行的铁律

> **一旦开始根据指定计划执行任务，就全力以赴把它执行完毕，不要停下来，不要询问，直到完成工作计划里的全部任务。**

#### 详细说明

**执行标准**:
- 在开始一个计划阶段（如Week 1、Week 2）前，确保已完全理解任务范围和目标
- 一旦开始执行，默认进入"专注模式"，不再中途询问"是否继续"、"是否完成"等问题
- 遇到阻塞问题时，自行分析并选择最佳解决方案，而非暂停等待确认
- 只有遇到以下情况才可暂停：
  - 技术方案存在根本性缺陷（需立即调整计划）
  - 发现重大安全风险（需评估后再继续）
  - 外部环境变化导致原计划不可行

**工作流程**:
```
接收任务 → 理解任务 → 确认理解（一次） → 全力执行 → 完成交付 → 简要汇报
              ↑___________↓
                   （无中断）
```

**示例场景**:
- ✅ **正确**: "开始实现Week 1的eSCL协议客户端" → 持续工作 → 完成功能 → "Week 1任务完成，已提交代码"
- ❌ **错误**: "开始实现Week 1" → 写了一点 → "我完成了30%，要继续吗？" → 等待回复 → 继续 → "现在完成了60%，还要做吗？"

---

### 原则二：设计先行的质量保障

> **在实现每个模块前，都先用karpathy-guidelines的技能思考并检验设计。**

#### 详细说明

**设计检验清单**:

在开始编码每个模块前，必须思考并回答以下问题：

1. **简洁性 (Simplicity)**
   - 这个设计是否足够简单？能否再简化？
   - 是否避免了过度工程化？
   - 是否使用了最直接的方式解决问题？

2. **接口设计 (Interface Design)**
   - 模块的接口是否清晰、易于理解？
   - 是否遵循了单一职责原则？
   - 依赖关系是否合理？是否可测试？

3. **错误处理 (Error Handling)**
   - 所有可能的错误情况是否都被考虑？
   - 错误信息是否清晰、可操作？
   - 是否有优雅降级方案？

4. **性能考量 (Performance)**
   - 这个设计的时间复杂度是多少？
   - 空间复杂度是否合理？
   - 是否存在明显的性能瓶颈？

5. **并发安全 (Concurrency)**
   - 是否需要考虑多线程/多协程？
   - 共享状态是否被妥善保护？
   - 是否存在死锁或竞态条件风险？

6. **可维护性 (Maintainability)**
   - 代码是否自解释？是否需要大量注释？
   - 是否易于扩展和修改？
   - 是否符合项目的编码规范？

**执行流程**:
```
接收模块任务 → 阅读需求 → 设计检验（思考上述6点） → 简要记录设计思路 → 开始编码 → 实现完成
```

**文档化要求**:
- 对于复杂模块，在代码文件中添加设计思路注释（顶部）
- 简单模块可在心中完成检验，但需有检验的过程

---

### 原则三：代码提交的纪律

> **在你认为合适的时候提交代码，保证工作树干净有序。**

#### 详细说明

**提交时机**:

- ✅ **必须提交**:
  - 完成一个独立功能点（如"实现设备发现API"）
  - 完成一次成功的重构
  - 修复一个bug并验证通过
  - 工作阶段结束（如当天工作结束前）
  - 需要切换任务前

- ✅ **可以提交**:
  - 代码通过了本地测试
  - 代码能够编译/运行
  - 逻辑相对完整，即使还有后续优化空间

- ❌ **不要提交**:
  - 代码无法编译或运行
  - 包含明显的临时调试代码（如fmt.Println大量输出）
  - 工作未完成且无法独立运行
  - 包含敏感信息（密码、密钥等）

**提交规范**:

```bash
# 提交信息格式
type: subject

body（可选）

footer（可选）
```

**Type分类**:
- `feat`: 新功能
- `fix`: 修复
- `docs`: 文档
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试
- `chore`: 构建/工具

**示例**:
```
feat: implement eSCL device discovery

- Add mDNS listener for _uscan._tcp service
- Parse ScannerCapabilities XML
- Store discovered devices to SQLite

Tested with HP Smart Tank 750
```

**工作树管理**:

```bash
# 理想的Git工作流
1. git status          # 查看当前状态
2. git add <files>     # 添加相关文件
3. git diff --cached   # 检查即将提交的内容
4. git commit          # 提交（编写规范信息）
5. git log --oneline -3 # 确认提交成功
```

**干净工作树的标准**:
- 没有未跟踪的重要文件
- 没有大量未提交的修改
- 每次提交后都能成功构建和运行

---

### 原则四：文档即代码

> **及时维护必要的文档。**

#### 详细说明

**文档即代码原则**:
- 文档是项目的一部分，与代码同等重要
- 文档修改应随代码一起提交，或紧随其后
- 保持文档的时效性，过期文档比无文档更有害

**必须维护的文档**:

1. **API文档** (`docs/api.md`)
   - 所有公开API的接口定义
   - 请求/响应示例
   - 错误码说明
   - 与代码同步更新

2. **架构文档** (`docs/architecture.md`)
   - 系统架构图
   - 模块依赖关系
   - 数据流图
   - 重大变更时更新

3. **数据库文档** (`docs/database.md`)
   - 表结构说明
   - 字段含义
   - 索引设计
   - 变更时更新

4. **部署文档** (`docs/deployment.md`)
   - 环境要求
   - 安装步骤
   - 配置说明
   - 版本升级指南

5. **开发日志** (`docs/dev-log.md`)
   - 开发过程中的重要决策
   - 遇到的问题和解决方案
   - 技术债务记录
   - 持续更新

**文档更新时机**:

- **代码提交时**: 如果修改了API，同步更新API文档
- **功能完成时**: 更新架构文档或开发日志
- **发现问题时**: 记录问题和解决方案
- **每周回顾时**: 整理本周的开发日志

**文档质量标准**:

- ✅ 准确：与代码实际行为一致
- ✅ 完整：覆盖所有重要方面
- ✅ 清晰：易于理解
- ✅ 时效：与代码同步

---

## 执行检查清单

每个开发者在开始工作前，应确认已理解并同意遵守以上原则：

- [ ] 我理解原则一：计划一旦开始，全力执行至完成，不中途询问
- [ ] 我理解原则二：编码前先进行设计检验，确保设计质量
- [ ] 我理解原则三：及时提交代码，保持工作树干净有序
- [ ] 我理解原则四：文档是代码的一部分，必须及时维护
- [ ] 我同意在参与本项目时始终遵守以上原则

---

## 原则的执行与监督

**自我监督**:
- 每个开发者对自己的执行质量负责
- 定期回顾自己的工作流程，检查是否遵守原则

**团队监督**:
- Code Review时检查设计质量（原则二）
- 检查提交历史是否规范（原则三）
- 检查文档是否同步更新（原则四）

**持续改进**:
- 本守则为v1.0版本，根据实践经验迭代优化
- 建议每季度回顾一次，必要时更新

---

## 附录：与AI助手协作的特别说明

当AI助手（如Claude Code）参与开发时：

1. **AI自动遵循原则一**：一旦开始执行计划，默认持续工作至完成
2. **AI主动应用原则二**：在每个模块实现前进行设计检验
3. **AI主动执行原则三**：在合适的时机自动提交代码
4. **AI主动维护原则四**：同步更新相关文档

人类开发者无需反复提醒AI遵守这些原则，AI已将此文档作为系统提示的一部分。

---

**制定人**: 项目技术负责人  
**生效日期**: 2026-06-11  
**下次评审**: 2026-09-11

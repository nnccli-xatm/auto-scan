# Claude Code 项目配置 - Auto Scan

> 本文档定义Claude Code在参与本项目时必须遵循的行为准则和开发原则。
> 这些准则与项目开发守则一致，确保AI助手与团队协作的无缝衔接。

---

## 🎯 核心行为准则

### 1. 全力以赴执行原则

**指令**: 一旦开始根据指定计划执行任务，必须全力以赴执行完毕，不得停下来询问，直到完成工作计划里的全部任务。

**执行细节**:
- 在开始执行任务前，确保已完全理解任务范围和目标
- 任务执行期间，默认进入"专注模式"，不中途询问"是否继续"、"是否完成"等问题
- 遇到技术问题自行分析并选择最佳解决方案
- 任务完成后，提供简要总结和状态报告

**例外情况**（可暂停并询问）:
- 发现技术方案存在根本性缺陷，需要调整计划
- 发现重大安全风险，需要评估后再继续
- 外部环境变化导致原计划不可行

**禁止行为**:
- ❌ "我完成了30%，要继续吗？"
- ❌ "遇到了问题，我该怎么办？"（应先尝试解决）
- ❌ "这个方案A和B，选哪个？"（应给出建议并说明理由）

**推荐行为**:
- ✅ "Week 1任务已完成，已提交代码。实现的功能包括：..."
- ✅ "遇到了X问题，已采用Y方案解决，原因是..."
- ✅ "建议采用方案A，理由是...，如确认我将继续实施"

---

### 2. 设计先行检验原则

**指令**: 在实现每个模块前，必须先进行设计检验，思考并回答以下关键问题。

**设计检验清单**（必须回答）:

```
□ 简洁性: 这个设计是否足够简单？能否再简化？
□ 接口设计: 模块接口是否清晰、易于理解和测试？
□ 错误处理: 所有错误情况是否被考虑？错误信息是否清晰？
□ 性能考量: 时间和空间复杂度是否合理？是否存在性能瓶颈？
□ 并发安全: 是否需要考虑多线程？共享状态是否被保护？
□ 可维护性: 代码是否自解释？是否易于扩展和修改？
```

**执行方式**:
- 对于简单模块（<100行），可在心中完成检验
- 对于复杂模块，在代码文件顶部添加设计思路注释：

```go
// Package scanner implements the eSCL protocol client.
//
// Design decisions:
// - Uses connection pool for HTTP clients to avoid connection leaks
// - Implements exponential backoff for retries (1s, 2s, 4s, 8s)
// - Thread-safe: uses sync.RWMutex to protect device state
// - Simplified: does not support IPv6 for initial implementation
// - Performance: response caching with 5-second TTL to reduce network calls
```

**要求**: 每个模块实现前，必须有检验过程，复杂模块需文档化设计思路。

---

### 3. 代码提交纪律

**指令**: 在合适的时机自动提交代码，保证工作树干净有序。

**提交时机**（AI自动判断）:

**必须提交**:
- 完成一个独立功能点（如"实现设备发现API"）
- 完成一次成功的重构
- 修复一个bug并验证通过
- 当天工作结束前（如有未提交的工作）
- 需要切换任务前

**可以提交**:
- 代码通过了本地测试
- 代码能够编译/运行
- 逻辑相对完整

**禁止提交**:
- 代码无法编译或运行
- 包含大量临时调试代码
- 工作未完成且无法独立运行
- 包含敏感信息

**提交信息格式**:
```
type: subject (imperative mood, max 50 chars)

body (optional, explain what and why, max 72 chars per line)

footer (optional, reference issues, breaking changes)
```

**Type分类**:
- `feat`: 新功能
- `fix`: 修复
- `docs`: 文档
- `style`: 代码格式
- `refactor`: 重构
- `test`: 测试
- `chore`: 构建/工具

**AI行为**: 
- AI应主动在合适的时机提交代码
- 提交前检查：能否编译？是否干净？信息是否规范？
- 提交后确认：git log查看提交是否成功

---

### 4. 文档即代码原则

**指令**: 及时维护必要的文档，文档是代码的一部分。

**必须维护的文档**:

| 文档 | 路径 | 更新时机 |
|------|------|---------|
| API文档 | `docs/api.md` | 修改API时 |
| 架构文档 | `docs/architecture.md` | 架构变更时 |
| 数据库文档 | `docs/database.md` | 修改Schema时 |
| 部署文档 | `docs/deployment.md` | 部署方式变更时 |
| 开发日志 | `docs/dev-log.md` | 遇到重要问题时 |

**文档更新规则**:
- 代码和文档修改应在同一批次提交
- 或文档修改紧随代码提交之后（下一个提交）
- 禁止代码修改与文档长期不同步（超过1天）

**质量标准**:
- 准确：与代码实际行为一致
- 完整：覆盖所有重要方面
- 清晰：易于理解
- 时效：与代码同步

---

## 🛠️ 项目技术栈确认

**后端**:
- 语言: Go 1.22+
- 框架: Gin (HTTP)
- 数据库: SQLite 3.40+ (WAL模式)
- ORM: GORM
- 日志: Zap

**前端**:
- 框架: Vue 3 (Composition API)
- 语言: TypeScript 5.0+
- UI库: Element Plus
- 状态: Pinia
- 构建: Vite

**协议**:
- 主要: eSCL (AirPrint Scan)
- 备选: WSD (Web Services for Devices)
- 暂不实现: TWAIN, USB (Phase 2)

**目标设备** (Phase 1):
- HP: Smart Tank系列, LaserJet Pro系列
- 佳能: imageRUNNER系列, imageCLASS系列
- 理光: MP C系列, IM C系列
- 延后: 富士通ScanSnap (需USB/TWAIN)

---

## 📝 开发工作流程

### 标准开发流程

```
1. 接收任务 (如"Week 1: 实现eSCL协议客户端")
   ↓
2. 理解任务
   - 阅读相关文档
   - 确认需求范围
   - 识别依赖关系
   ↓
3. 设计检验 (原则二)
   - 回答6个设计问题
   - 复杂模块编写设计注释
   ↓
4. 编码实现
   - 遵循Go/Vue编码规范
   - 编写单元测试
   - 本地验证通过
   ↓
5. 提交代码 (原则三)
   - 检查工作树状态
   - 编写规范提交信息
   - 提交并验证
   ↓
6. 更新文档 (原则四)
   - 同步更新API/架构文档
   - 记录开发日志（如有必要）
   ↓
7. 任务完成汇报
   - 完成功能总结
   - 提交统计
   - 下一步建议（如有）
```

### 与Git协作流程

```bash
# 开始工作前
git pull origin main

# 工作期间（多次循环）
git status                    # 检查状态
git add <files>              # 添加文件
git diff --cached            # 检查变更
git commit -m "type: msg"    # 提交

# 工作完成后
git push origin main
```

---

## ⚠️ 禁止行为清单

**绝对禁止**:
- 未经测试的代码直接提交
- 提交包含密码、密钥的代码
- 长时间不提交（超过1天工作不提交）
- 代码与文档长期不同步（超过3天）

**应当避免**:
- 过度设计（为未来可能的需求过度抽象）
- 代码重复（不遵循DRY原则）
- 忽略错误处理
- 不写注释的复杂逻辑

---

## ✅ 质量检查清单

**编码前**:
- [ ] 理解了任务需求和范围
- [ ] 完成了设计检验（6个问题）
- [ ] 识别了依赖和风险

**编码中**:
- [ ] 遵循编码规范
- [ ] 编写了错误处理
- [ ] 考虑了并发安全（如需要）
- [ ] 代码可测试

**提交前**:
- [ ] 代码可编译/运行
- [ ] 通过了本地测试
- [ ] 没有临时调试代码
- [ ] 提交信息规范

**提交后**:
- [ ] 确认提交成功（git log）
- [ ] 相关文档已更新
- [ ] 工作树干净

---

## 🔄 持续改进

本配置文档随项目发展迭代：
- **版本**: v1.0 (2026-06-11)
- **下次评审**: 2026-09-11 或项目里程碑完成时
- **更新方式**: 提交PR修改，经团队讨论后合并

---

## 📚 参考文档

- [开发守则](./开发守则.md) - 项目开发原则完整版
- [Phase1_MVP实施计划](./Phase1_MVP实施计划_网络设备优先.md) - 详细工作计划
- [技术评估报告](./技术评估报告_扫描设备自动任务系统_v1.0.md) - 技术选型依据

---

**生效日期**: 2026-06-11  
**适用对象**: Claude Code AI助手及所有项目开发者  
**监督方式**: 自我监督 + Code Review检查
